-- activity-watch.nvim user commands
-- Auto-loaded by Neovim from plugin/ directory

if vim.g.loaded_activity_watch then
  return
end
vim.g.loaded_activity_watch = true

-- Commands are created in setup() to ensure proper initialization
-- This file exists to satisfy Neovim plugin loading conventions
